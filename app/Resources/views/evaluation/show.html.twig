{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evaluation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ evaluation.value }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if evaluation.createdAt %}{{ evaluation.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evaluation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('evaluation_edit', { 'id': evaluation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
