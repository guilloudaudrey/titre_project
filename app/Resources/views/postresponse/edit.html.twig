{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container container_post_response_edit">
        <div class="panel panel-info">
            <div class="panel-heading">
                <h1 class="panel-title">Postresponse edit</h1>
            </div>
            <div class="panel-body">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
                <div class="buttons_edit_post_response">
                    <input class="btn btn-info" type="submit" value="Modifier" />
                    {{ form_start(delete_form) }}

                    <button  type="button" class="btn btn-default btn-sm button_delete_post_response" data-toggle="modal" data-target="#modal_delete_post_response">Supprimer</button>
                    {% include 'postresponse/modal_delete_post_reponse.html.twig' %}
                    {{ form_end(delete_form) }}
                </div>
    {{ form_end(edit_form) }}


            </div>
    </div>
        <a class="btn btn-default" href="{{ path('post_proofreader_show', { 'id': postResponse.getPost().getId() }) }}">Annuler</a>
    </div>
{% endblock %}
