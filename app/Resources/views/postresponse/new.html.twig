{% extends 'base.html.twig' %}

{% block content %}
    {% include 'menu.html.twig' %}
    <div class="container">
        <h1>Réponse</h1>
        {{ post.text }}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
    </div>
    <ul>
        <li>
            <a href="{{ path('postresponse_index') }}">Retour</a>
        </li>
    </ul>
{% endblock %}
